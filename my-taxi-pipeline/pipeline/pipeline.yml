name: nyc-taxi-pipeline

schedule: monthly

start_date: "2025-01-01"

default_connections:
  duckdb: motherduck-prod

variables:
  taxi_types:
    type: array
    items:
      type: string
    default: ["yellow", "green"]
